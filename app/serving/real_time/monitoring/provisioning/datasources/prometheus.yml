apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # For Docker Compose: use service name
    # For Kubernetes: use http://prometheus:80
    url: http://prometheus:9090
    uid: prometheus
    isDefault: true
    editable: false
